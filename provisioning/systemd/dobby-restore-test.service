[Unit]
Description=dobby monthly restore-test (prove the archive is recoverable)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/dobby
ExecStart=/usr/bin/docker compose run --rm archive-job restore-test
