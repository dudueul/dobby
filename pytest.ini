[pytest]
# Tests read as English sentences in subject_verb_condition form, so collect
# camelCase function names with an underscore (e.g. evictionPlan_evictsOldestFirst)
# in addition to the conventional test_* prefix.
python_files = test_*.py
python_functions = test_* *_*
